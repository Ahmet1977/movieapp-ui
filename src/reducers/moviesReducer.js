// Create with rxreducer snippet
const initialState = {

}

export default (state = [], { type, payload }) => {
    switch (type) {

  /*  case typeName:
        return { ...state, ...payload } */

    default:
        return state
    }
}
